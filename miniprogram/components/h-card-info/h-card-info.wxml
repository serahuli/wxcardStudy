<view class="id-info">
  <view>姓名：{{ info.name }}</view>
  <view>出生日期：{{ info.birth }}</view>
  <view>性别：{{ info.sex }}</view>
  <view>地址：{{ info.address }}</view>
  <view>民族：{{ info.nation }}</view>
  <view>身份证号码：{{ info.id }}</view>
  <image wx:if="{{ showIamge }}" src="{{ info.fileID }}" mode="widthFix" />
  <!-- <image 
    src="{{ imgTest }}" 
    mode="widthFix" /> -->
</view>